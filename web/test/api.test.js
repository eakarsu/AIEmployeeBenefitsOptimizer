const test=require('node:test');const assert=require('node:assert/strict');const{createClient,currency,normalizeList}=require('../public/api');
test('normalizes enrollment pages and formats plan cost',()=>{assert.deepEqual(normalizeList({data:[{id:4}]}),[{id:4}]);assert.match(currency(1250.5),/1,250\.50/);});
test('benefits client sends authorization for enrollment writes',async()=>{let options;const client=createClient({baseUrl:'http://api.test',getToken:()=> 'hr-token',fetchImpl:async(_url,value)=>{options=value;return{ok:true,status:201,json:async()=>({status:'pending'})};}});const result=await client('/enrollment',{method:'POST',body:JSON.stringify({employeeName:'A User'})});assert.equal(result.status,'pending');assert.equal(options.headers.Authorization,'Bearer hr-token');assert.equal(options.headers['Content-Type'],'application/json');});
